//6 inference 
// in typescript automatically know anout our type for eg if we give 
let num =5;
//it automatically gives our type into number it should necessar to define it type number

let num1 ="hemanta";
//it automatically gives our type into string it should necessar to define it type string